// Runtime button recoloring was removed because it caused visible color flashes
// during page load and when dynamic content rendered. Button appearance is now
// fully controlled by static CSS.
export function initButtonStyles() {
  // Intentionally no-op.
}
